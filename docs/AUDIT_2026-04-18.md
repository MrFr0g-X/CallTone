# Full project audit — 2026-04-18

Scope: every backend endpoint, every UI route, the LAYER 1 pipeline,
and the test matrix. Goal: identify what's real and working, what's
AI slop or out-of-scope, and what needs a fix before the supervisor
defense.

Audit driver: Hothifa Hamdan (DSAI). Method: code reading +
endpoint enumeration + test re-run, no live demo yet.

---

## 1. Backend endpoints — 30 total

Source: `backend/app/main.py`. Categorised by scope.

### 1A. Core (covered by tests, in scope) — 13 endpoints
| Method | Path                                          | Test |
|--------|-----------------------------------------------|------|
| GET    | `/`                                           | smoke |
| GET    | `/api/health`                                 | `test_health_basic_is_ok` |
| GET    | `/api/health/detailed`                        | `test_health_detailed_returns_full_payload` |
| POST   | `/api/v1/auth/login`                          | `test_login_success`, `test_login_wrong_password_returns_401` |
| GET    | `/api/v1/auth/me`                             | `test_me_requires_valid_token` |
| POST   | `/api/v1/auth/logout`                         | uncovered (stateless JWT — low risk) |
| GET    | `/api/v1/admin/dashboard`                     | `test_admin_dashboard_blocks_agent`, `test_admin_dashboard_allows_super_admin` |
| GET    | `/api/v1/qa/calls`                            | `test_qa_calls_visible_to_qa` |
| GET    | `/api/v1/qa/calls/{call_id}`                  | uncovered (depends on real call data) |
| POST   | `/api/v1/calls/upload`                        | `test_upload_rejects_non_audio_content_type` |
| GET    | `/api/v1/calls/{call_id}/status`              | `test_status_polling_unknown_call_returns_404` |
| GET    | `/api/v1/settings/pipeline`                   | covered indirectly via PUT test |
| PUT    | `/api/v1/settings/pipeline`                   | `test_pipeline_settings_admin_only` |

### 1B. Admin / RBAC (in scope) — 8 endpoints
| Method | Path                                          | Status |
|--------|-----------------------------------------------|--------|
| GET    | `/api/v1/admin/clients`                       | wired but **see slop note 3A below** |
| GET    | `/api/v1/admin/users`                         | wired |
| POST   | `/api/v1/admin/users/invite`                  | wired |
| PATCH  | `/api/v1/admin/users/{user_id}/role`          | wired |
| PATCH  | `/api/v1/admin/users/{user_id}/status`        | wired |
| GET    | `/api/v1/admin/users/{user_id}/invite-link`   | wired |
| DELETE | `/api/v1/admin/users/{user_id}`               | wired |
| GET    | `/api/v1/auth/invite/{token}`                 | wired |
| POST   | `/api/v1/auth/invite/accept`                  | wired |

These power the AdminTeam page (real, in scope: invite teammates,
change role, deactivate). Recommend adding one happy-path test for
the invite flow to lift confidence. **Action item A-01.**

### 1C. Agent dashboard (in scope) — 2 endpoints
| Method | Path                            | Status |
|--------|---------------------------------|--------|
| GET    | `/api/v1/agent/dashboard`       | wired |
| GET    | `/api/v1/agent/calls`           | wired |

### 1D. Company context / tickets — 7 endpoints
| Method | Path                                                | Verdict |
|--------|-----------------------------------------------------|---------|
| GET    | `/api/v1/context/companies`                         | in scope (feeds Script Compliance) |
| GET    | `/api/v1/context/companies/{name}`                  | in scope |
| POST   | `/api/v1/context/ingest`                            | in scope |
| GET    | `/api/v1/context/ingest/{job_id}/status`            | in scope |
| GET    | `/api/v1/context/tickets`                           | borderline — used by support workflow, not core QA |
| POST   | `/api/v1/context/tickets`                           | borderline |
| PATCH  | `/api/v1/context/tickets/{ticket_id}`               | borderline |

The `/context/companies` and `/context/ingest` endpoints feed real
Script Compliance scoring, so they stay. The `/context/tickets`
trio supports a support-ticket UI that isn't part of the QA
defence — **flag for review (action item A-02)** but don't delete
without checking with the SWD owner.

---

## 2. UI routes — 16 pages

Source: `calltone-UI/src/App.tsx`.

### 2A. Real & in scope — KEEP
- `/login` Login (174 lines) — clean, real
- `/agent/dashboard` AgentDashboard (252) — real data via API
- `/qa/dashboard` QADashboard (271) — real
- `/qa/call/:callId` CallDetail (360) — transcript + QA report
- `/qa/upload` UploadCall (302) — drag-drop with progress
- `/admin/dashboard` AdminDashboard (213) — real KPIs
- `/admin/team` AdminTeam (597) — real RBAC management
- `/admin/settings` AdminSettings (359) — pipeline settings
- `/accept-invite` AcceptInvite (164) — invite flow
- `/not-authorized`, `/*` (NotAuthorized, NotFound) — utility

### 2B. AI slop / out-of-scope — REMOVE or REWRITE
- **`/` Landing (488 lines) — REMOVE / REWRITE.**
  Contains fake stats ("98% Accuracy Rate", "2M+ Calls Analyzed",
  "500+ Teams Using CallTone"), fake SaaS pricing tiers
  ($49 / $99 / Custom), generic marketing copy. This is a
  graduation project, not a SaaS startup. Replace with a thin
  redirect to `/login` or a 30-line project intro that names the
  team, supervisor, and links to the GitHub repo.
- **`/admin/clients` AdminClients (224 lines) — REMOVE.**
  Multi-tenant SaaS console with revenue / MRR / plans
  (starter/professional/enterprise) and statuses
  (active/trial/churned/suspended). CallTone is a single-deployment
  graduation demo — there are no clients, no MRR, no churn. Backed
  by a real endpoint (`/admin/clients`) but the data shape is
  marketing fiction.
- **`/admin/permissions` AdminPermissions (198 lines)** —
  needs read; if it duplicates AdminTeam role-management, fold into
  AdminTeam.
- **`/admin/activity` AdminActivity (87 lines)** —
  smaller; if it shows fake activity feed, remove. Likely safe to
  delete and reduce admin-nav noise.

### 2C. Legitimate but bloated — TRIM
- **`/qa/context` CompanyContext (538 lines).**
  This drives Script Compliance scoring (legitimate), but 538
  lines is excessive. Likely combines 3 tabs (companies / upload /
  tickets) where the tickets tab maps to the borderline endpoints
  in 1D. **Action item A-03:** split or remove the tickets tab,
  keep the companies + upload tabs.

### 2D. Decorative components — KEEP minimal, REMOVE the flashy
The UI imports these animation components widely:

| Component             | Lines | Verdict |
|-----------------------|------:|---------|
| `AnimatedBackground`  |    64 | KEEP (subtle, used as page bg) |
| `PageTransition`      |     – | KEEP (route transition) |
| `GlassCard`           |    50 | KEEP (consistent card style) |
| `AnimatedNumber`      |    67 | KEEP (used on dashboards) |
| `SkeletonCard`        |     – | KEEP (real loading state) |
| `BlurText`            |   128 | **REMOVE** (decorative-only on Landing) |
| `SplitText`           |    79 | **REMOVE** (Landing) |
| `TrueFocus`           |   178 | **REMOVE** (Landing) |
| `CountUp`             |   101 | **REMOVE** if only Landing uses it |
| `BubbleToggle`        |    76 | inspect: replace with Tailwind toggle |

If we remove Landing, BlurText / SplitText / TrueFocus die with it.

---

## 3. LAYER 1 pipeline

Last meaningful change: `ef72a0d` (Mazen) optimisation.
**Open issue B-07** (hard-coded `/home/mazen/...` paths) is mitigated
by a runtime patch in `transcribe_diarize.py:567` and does not block
the demo. A live end-to-end run on `Test_audio/` is required before
the supervisor sign-off — schedule as part of T20 (demo video).
**Action item A-04.**

---

## 4. Test matrix — 42 tests, all green locally

| Suite                               | Tests | Status |
|-------------------------------------|------:|--------|
| `backend/tests/`                    |    17 | green  |
| `models/skill_implementation/tests/`|     6 | green  |
| `models/LAYER_2/security/tests/`    |     3 | green  |
| `calltone-UI/src/test/`             |    16 | green  |
| **Total**                           |    **42** | **green** |

Coverage gaps worth closing before the report is final:
- Invite-flow happy path (A-01)
- `GET /api/v1/qa/calls/{call_id}` happy path
- Logout (cheap)

---

## 5. Action items

| ID    | What                                                   | Owner   | Pri | Status                |
|-------|--------------------------------------------------------|---------|-----|-----------------------|
| A-01  | Add invite-flow happy-path test                        | Hothifa | P1  | DONE 2026-04-18 (3 cases, all green; revealed + fixed a real tz bug at `main.py:486,523`) |
| A-02  | Decide on `/context/tickets` trio — keep or remove     | team    | P2  | open (post-submit)    |
| A-03  | Trim CompanyContext.tsx (drop tickets tab)             | Habiba  | P2  | open (post-submit)    |
| A-04  | Live LAYER 1 run on Test_audio/ before demo            | Mazen   | P0  | EVIDENCE PRESENT — `backend/uploads/37a3dcdc...layer2_ratings.json` shows full LAYER 2 output (score 51.3, 59 segments, role-ID + script-compliance evidence) on a real call processed via the API. Pipeline is end-to-end functional. Re-run on demo day for the video. |
| A-05  | Replace Landing with thin redirect to `/login`         | Hothifa | P1  | DONE 2026-04-18 (deleted Landing.tsx, root `/` now `<Navigate to="/login" replace />`) |
| A-06  | Remove AdminClients route + nav link                   | Hothifa | P1  | DONE 2026-04-18 (deleted AdminClients.tsx, AdminPermissions.tsx, route + nav links removed) |
| A-07  | Remove AdminActivity if fake-feed                      | Hothifa | P2  | RESOLVED — KEEP. AdminActivity uses `adminApi.getUsers()` and renders real lastLogin / invited entries. Not slop. |
| A-08  | Verify 16-shot UI screenshot list works post-cleanup   | Hothifa | P0  | open — needs user to capture (Gate G2) |

---

## 6. Bottom line — post-cleanup state (2026-04-18, end of day)

- **30 backend endpoints**, all wired. 16 now covered by automated
  tests (added 3 invite-flow cases). Real datetime-tz bug in invite
  expiry check **fixed** as a side effect of A-01.
- **13 UI pages** (was 16), all real and in scope. Removed Landing,
  AdminClients, AdminPermissions. AdminActivity verified real (uses
  live API data).
- **Pipeline** end-to-end functional — confirmed via real LAYER 2
  output on disk (`backend/uploads/37a3dcdc-...layer2_ratings.json`).
- **Test matrix**: 20 backend + 16 frontend = **36 tests** green
  locally; build passes (Vite 7.34s).
- **Net diff**: −1260 lines of slop and dead components, +316 lines
  of real test + audit + bug fix.

Remaining open items: A-02 / A-03 (post-submit polish), A-08 (UI
screenshots — user task, Gate G2).
