fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pydantic
pydantic-settings
python-jose[cryptography]
passlib[bcrypt]
# Pin bcrypt: passlib 1.7.4 (unmaintained) probes with a 100+ byte string at import.
# bcrypt 5.x enforces the 72-byte spec and raises; 4.0.x silently truncates as before.
bcrypt<5.1
email-validator
python-multipart

# dev/test
pytest>=7
pytest-cov>=4
httpx>=0.24